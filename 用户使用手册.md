# 高中教育信通版智能体——用户使用手册

**版本号：** v1.0
**适用对象：** 高中信息技术与通用技术课程教师、学生与管理员
**最后更新：** 2026 年 5 月 16 日

---

## 目录

1. [系统概述](#系统概述)
2. [快速开始](#快速开始)
3. [用户角色与权限](#用户角色与权限)
4. [登录与界面说明](#登录与界面说明)
5. [主要功能说明](#主要功能说明)
6. [教师与管理员操作](#教师与管理员操作)
7. [学生使用说明](#学生使用说明)
8. [文件与资源管理](#文件与资源管理)
9. [任务与积分系统](#任务与积分系统)
10. [对话历史与导出](#对话历史与导出)
11. [技术配置与常见问题](#技术配置与常见问题)

---

## 系统概述

教育智能体-高中信通版（SmartKB）是一款面向高中信息技术与通用技术课程的AI智能问答与教学管理平台。系统集成了本地用户管理、角色权限、AI对话、多模态文件处理、教学任务、历史记录和HTML资源管理。

系统支持三种角色：管理员、教师、学生。管理员与教师可管理教学资源、创建任务、查看课堂积分；学生主要使用智能对话、完成任务、查看个人历史。

---

## 快速开始

1. 进入项目目录：

   ```powershell
   cd /d d:\AgentSmartKB
   ```

2. 安装依赖：

   ```powershell
   pip install -r requirements.txt
   ```

3. 启动应用：

   ```powershell
   python AgentSmartKBXS.py
   ```

4. 默认管理员登录：
   - 用户名：`root`
   - 密码：`root`

> 如果需要使用云端 DashScope 服务，请先在 `.env` 文件中配置 `dashscope_api_key` 和 `deepseek_api_key`。

---

## 用户角色与权限

### 管理员（root）

- 系统配置与维护
- 用户注册、删除、批量导入、批量删除
- 查看所有用户信息
- 管理HTML资源、课堂积分、任务汇总
- 可查看管理员目录下的所有任务和汇总文件

### 教师

- 创建教学任务
- 查看学生提交情况和任务汇总
- 上传和管理 HTML 教学资源
- 访问课堂积分页面
- 参与用户管理（仅修改个人信息与密码）

### 学生

- 参与 AI 对话答疑
- 上传文档与图片进行问答
- 查看个人历史对话
- 提交任务
- 使用代码实时预览功能

---

## 登录与界面说明

### 登录方式

系统支持“用户名登录”和“姓名登录”。

- 当姓名在系统中唯一时，可直接使用姓名登录。
- 若姓名重复，系统会提示请改用用户名登录。
- 登录成功后，会在界面顶部显示当前用户、班级、姓名、性别和在线人数。

### 登录失败常见原因

- 用户名或姓名不存在
- 密码错误
- 同名用户存在时未使用用户名登录

### 登录后界面结构

#### 教师 / 管理员界面

- AI对话区域：输入问题，发送后与AI互动
- 文件上传区域：支持文档和图像上传
- 历史记录文件浏览器：按用户目录和日期查看历史
- HTML资源区：教师/管理员可以访问个人HTML教学资源目录
- 课堂积分/点名页：管理员与教师可查看积分与班级点名

#### 学生界面

- AI对话输入区
- 多模态文件上传区
- 历史记录浏览区
- 学生可查看自己的ChatHistory目录，不可访问他人目录

---

## 主要功能说明

### AI 对话与多模态输入

系统使用 DashScope 云端AI，并支持：

- 文本问答
- 文档理解
- 图像理解
- 文件摘要增强对话

AI对话会自动保存到用户目录中的 `ChatHistory/<日期>/conversation_*.md` 文件中。

### 文件支持

文档格式：`.txt .md .pdf .doc .docx .xls .xlsx .ppt .pptx .csv .json .html .htm`

图像格式：`.jpg .jpeg .png .gif .bmp .tiff .webp`

大小限制：

- 文档最大 10MB
- 图像最大 5MB

### HTML资源预览

管理员与教师可上传和管理HTML文件，系统会自动展示HTML资源列表并支持直接打开预览链接。

### 任务系统

教师和管理员可以创建任务，学生可以通过“完成”或“结束”提交任务。

- 任务创建：输入 `提交 [任务名称] 任务`
- 任务提交：输入 `完成` 或 `结束`
- 多任务情况下，可输入任务编号选择提交目标

任务数据保存在：

- `root/ChatHistory/Task/<creator>/active_tasks.json`
- `root/ChatHistory/Task/all_active_tasks.json`
- `root/ChatHistory/Summary/teachers/<creator>/summary_<task>.md`
- `root/ChatHistory/Summary/admin/summary_<creator>_<task>.md`

---

## 教师与管理员操作

### 用户管理

管理员可以：

- 注册新用户
- 删除用户
- 修改用户信息
- 修改密码
- 批量导入用户（CSV）
- 批量删除用户

普通用户可以：

- 修改个人信息
- 修改个人密码

#### CSV 批量导入

系统支持 CSV 导入模板，字段包括：

```csv
username,password,class,name,gender,role
```

示例：

```csv
s11001,Password123,1,张三,男,普通用户
```

### 课堂 HTML 资源

教师与管理员的资源目录为：

- 管理员：`root/html`
- 普通用户：`<username>/html`

资源面板会自动展示当前用户 HTML 目录下的文件，并为每个文件生成可点击预览链接。

### 课程积分与点名

系统集成课堂积分与公平点名API：

- `score_system.py` 提供积分查询、排行、统计、加分、重置
- `smart_rollcall_api.py` 提供点名、权重公平算法、历史记录、点名标记

仅管理员和教师可访问课堂积分与点名模块。

### 任务创建与管理

#### 创建任务

在对话框中输入：

```text
提交 Python 列表综合应用任务
```

系统会在教师或管理员的活动任务列表中新增任务，并生成统一任务文件 `all_active_tasks.json`。

#### 查看任务

教师和管理员可在自己的任务目录下查看当前活动任务列表；学生通过智能筛选获得相关任务。

#### 提交任务

学生输入：

```text
完成
```

或

```text
结束
```

系统自动匹配学生所属班级相关任务，并将当前对话保存为任务提交汇总。

如果存在多个活动任务，系统会提示：

```text
请输入任务编号（1-...）
```

---

## 学生使用说明

### AI问答基本流程

1. 在输入框输入问题
2. 点击“发送”或按 Enter
3. 等待AI回答
4. 若不清楚，可继续追问

### 示例问题

```text
什么是算法的基本结构？
Python 中的列表有哪些常用方法？
通用技术的结构设计原则是什么？
请解释这个图表的含义。
```

### 文件与图像上传

1. 点击上传按钮
2. 选择文档或图像文件
3. 发送问题

系统会自动识别文件类型并对文档/图像进行处理。

### 任务提交

- 浏览任务内容
- 与AI讨论完成任务
- 输入 `完成` 或 `结束`
- 若系统提示选择编号，输入任务序号即可

> 系统会将提交内容保存到教师和管理员的任务汇总文件中。

### 历史记录查看

学生可通过“我的历史”或文件浏览器查看个人对话历史，历史记录按日期保存。

---

## 文件与资源管理

### 文件上传说明

- 文档支持：TXT、MD、PDF、DOC、DOCX、XLS、XLSX、PPT、PPTX、CSV、JSON、HTML
- 图像支持：JPG、JPEG、PNG、GIF、BMP、TIFF、WEBP
- 文档大小上限：10MB
- 图像大小上限：5MB

### 资源目录说明

- 用户对话历史：`<用户名>/ChatHistory/<日期>/conversation_*.md`
- 管理员汇总：`root/ChatHistory/Summary/...`
- 教学资源：`<用户名>/html/` 或 `root/html/`
- 活动任务：`root/ChatHistory/Task/<creator>/active_tasks.json`

### HTML预览功能

系统会检测AI输出中的HTML代码块，并自动启用内嵌预览。支持：

- ` ```html ... ``` ` 代码块
- 完整 HTML 文本
- 动态页面预览

---

## 任务与积分系统

### 活动任务存储

任务及其状态保存在管理员统一目录：

- `root/ChatHistory/Task/<creator>/active_tasks.json`
- `root/ChatHistory/Task/all_active_tasks.json`

每个任务会记录：

- 创建者
- 任务名称
- 任务ID
- 创建时间
- 提交学生列表

### 提交汇总

学生提交后，系统会自动写入：

- `root/ChatHistory/Summary/teachers/<creator>/summary_<task>.md`
- `root/ChatHistory/Summary/admin/summary_<creator>_<task>.md`

汇总文件包含学生提交时间和对话内容。

### 积分与点名

教师与管理员可使用积分系统查看：

- 班级积分排行
- 学生积分统计
- 年级/班级分数总览

点名系统使用公平权重算法，自动维护：

- 每位学生被点名的权重
- 正确/错误答题加分
- 历史点名记录

---

## 对话历史与导出

### 自动保存

AI对话会自动追加保存到当前用户的 `ChatHistory/<日期>/conversation_*.md` 文件中。

### 查看历史

通过文件浏览器选择历史文件查看内容。

### 删除历史

用户仅能删除自身目录下的历史文件。管理员可管理所有用户历史。

### HTML预览历史

如果历史内容包含HTML代码块，系统会显示“HTML预览”按钮，直接预览页面效果。

---

## 技术配置与常见问题

### 配置说明

- `users.db` 存储用户账户数据
- `root/html` 存储管理员教学资源
- `root/ChatHistory` 存储任务与汇总文件
- `.env` 存放 `dashscope_api_key` 与 `deepseek_api_key`

### 常见问题

**Q: 忘记密码怎么办？**
A: 请管理员登录后进入用户管理面板重置用户密码。

**Q: 登录失败提示“姓名重复”？**
A: 说明系统中有多个同名用户，请改用用户名登录。

**Q: 为什么看不到“教学资源”页？**
A: 只有管理员和教师角色可以查看HTML资源与课堂积分页面。

**Q: 文件上传失败？**
A: 请检查文件大小和格式是否符合限制。

**Q: AI回答不准确？**
A: 尝试上传相关文件启用上下文增强，或使用更明确的问题描述。

---
